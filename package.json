{
  "name": "portalescape",
  "description": "Simple app server for the Google App Engine Standard Environment to run the game.",
  "version": "0.0.1",
  "private": true,
  "license": "ISC",
  "author": "Christian Wiggins",
  "repository": {
    "type": "git",
    "url": ""
  },
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "system-test": "repo-tools test app",
    "test": "npm run system-test",
    "prepare": "npm run gcp-build",
    "gcp-build": "cd game; npm install; cd ..",
    "localserver": "node localserver/localserver.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "hammerjs": "^2.0.8"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^3.0.0"
  },
  "cloud-repo-tools": {
    "test": {
      "app": {
        "msg": "Hello, world!"
      }
    },
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
